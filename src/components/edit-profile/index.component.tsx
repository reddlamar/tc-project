import {View} from 'react-native';
import React from 'react';
import {TextInput, Text, Button} from 'react-native-paper';

// import {styles} from './styles.component';

const EditProfile = ({children}: any) => {
  return <>{children}</>;
};

export default EditProfile;
