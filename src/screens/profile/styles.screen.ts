import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    // flex: 1,
    // margin: 12,
  },
  cardContainer: {
    height: '100%',
    paddingHorizontal: 12,
  },
  image: {borderRadius: 50, borderWidth: 3},
});
